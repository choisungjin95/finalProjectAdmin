<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jhta.mybatis.mapper.film">
	<select id="getinfo" parameterType="int" resultType="film">
		select * from film where filmNum=#{filmNum} 
	</select>
	<resultMap type="hashMap" id="getResultMap">
		<id column="t.totalPeople" property="totalPeople"/>
		<id column="t.totalPrice" property="totalPrice"/>
		<id column="t.totalDate" property="totalDate"/>
		<id column="f.fileName" property="fileName"/>
	</resultMap>
	
	<select id="getIncome" parameterType="string" resultMap="getResultMap">
		select t.totalPeople,t.totalPrice,t.totalDate,f.fileName from total t,film f where f.filmName=#{fileName} 
		and t.filmNum = f.fileNum
	</select>
	
	
</mapper>